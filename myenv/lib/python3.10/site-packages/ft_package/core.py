def count_in_list(lst, elem):
    """
    Counts the number of times item appears in lst.
    
    Args:
        - lst : the list to be checked.
        - item : the item to be counted in the list.
    """
    return lst.count(elem)